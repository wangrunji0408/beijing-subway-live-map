# 实现细节

本文档记录数据格式、解析方法、校验规则以及各线路的特殊处理。
项目总览见 [README.md](README.md)。

## 数据格式

全部 JSONL（`work/parsed/*.jsonl`、`work/out/*.jsonl`、`output/*.jsonl`）都用
**紧凑格式**：逐行重复的字段名不再写出，23 MB + 17 MB → 3.3 MB + 2.5 MB（14%），
且可无损还原。编解码集中在 `work/parse/compact.py`，读写双方都经它转换，
所以解析、反推、检查、建图等逻辑代码看到的仍是原来的展开结构。

### timetables.jsonl

```json
{
  "id": "1-万寿路-1",
  "line": "1",
  "station": "万寿路",
  "direction": "环球度假区",
  "service": "weekday",
  "suffix": "1",
  "source": "timetables/1-万寿路-1.jpg",
  "legend": {"red": "四惠东", "yellow": "土桥"},
  "times": [[5, 11, "red"], [5, 15, "red"], [5, 19]]
}
```

`times` 的每一项是 `[时, 分, 颜色?]`：颜色只在站牌上有圆环/实心圆标记时出现
（`legend` 把它映射到终点站）；`terminal` 字段在全部数据中恒为 null，故省略。

- `direction`：站牌上「开往 X 站方向」的 X。环线（2、10 号线）与机场线为下一站名。
- `service`：运营日代号，见下文「运营日筛选」。
- `color`：该班次在图上圆环/实心圆点的颜色，对应 `legend` 中的终点站。
- 一张图片含两个时刻表时（如 18 号线），输出两条记录，`id` 形如 `<base>#0`、`<base>#1`。

### train_runs.jsonl

```json
{
  "line": "1", "group": 1, "direction": "环球度假区", "service": "weekday",
  "station_order": ["四惠东", "四惠", "..."],
  "tau": [0.0, 3.2, "..."],
  "total_travel": 55.2, "n_stations": 23, "n_runs": 278,
  "runs": [[0, 304, 1, 307], [0, 312, 1, 315]]
}
```

`tau` 与 `station_order` 一一对应，是各站相对始发站的累计时分。
每趟车是**扁平的 `[站序号, 分钟, 站序号, 分钟, …]`**，`minute` 为自当日 0 点起的
分钟数。短交路列车在中途站自然终止（数组较短），其余列车跑满全程。

## 解析方法（`work/parse/`）

1. **图像预处理**（`common.py`）：CMYK/截断 JPEG 归一化；Otsu + 背景极性判定得到"墨迹"掩膜。
2. **字形提取**：连通域分析区分数字与圆环/实心圆标记；圆环内部用腐蚀 + Otsu 提取数字，
   实心圆用孔洞提取，兼容深色字/浅色字两种配色。
3. **行/列还原**：按 y 聚类成小时行，按 x 间隙聚类成分钟数字，识别左侧小时列。
4. **数字识别**（`parse_line.py`）：把整行数字重绘成干净的"白底黑字"图，横向分块后交给
   tesseract（`psm 7`，整面板一次 `psm 6` 批量识别）；利用「分钟恒为两位、小时为 1–2 位」
   的约束做对齐，并同时尝试字形计数对齐，取合法分钟最多者。
5. **质量校验**（`checker.py`）：检查分钟范围、行内单调、重复、相邻班次间隔（默认 2–15 分钟，
   低峰线路自适应放宽），输出 `report.json` 供人工复核。
6. **运行图反推**（`infer_diagram.py`）：以 OSM 站序为骨架，按物理方向分组，用各站时刻
   校准站间时分，再将始发站每一班车延伸到沿线各站。
7. **地图数据**（`build_web_data.py`）：OSM 线网几何（WGS84）+ 站点吸附到线路，输出 `web/data/network.json`。

## 运行图校验工具

```bash
python3 work/parse/check_diagram.py --allow 6   # 超车检查（6 号线有大站快车，豁免）
python3 work/parse/check_speed.py               # 区间速度检查（绝对区间 + 逐线离群）
python3 work/parse/check_reverse.py --tol 2     # 由运行图反向导出时刻表，与原时刻表对比
```

### `check_diagram.py` — 超车检查

同一线路同一方向的列车按发车顺序不得互相超越（含站间交叉检测）。当前 **0 起超车**。
6 号线因同时开行大站快车与普通车，允许超车，故豁免。

### `check_speed.py` — 区间速度检查

速度按**运行时间**计算：站牌上的分钟是到站时刻，列车停站 1 分钟，所以区间运行时间 =
相邻站时刻差 − 1 分钟（与地图动画一致）。官方站间距除以运行时间得到区间平均速度，
低于 **20 km/h** 或高于 **120 km/h**（机场线豁免上限）即判有误；短区间给 1.5 分钟停站余量。
当前 **0 起越界**（修复前 15 号线 `南法信→后沙峪` 4.6 km 仅 1 分钟 = 275 km/h）。

另按**每条线自身的速度中位数**判定离群区间（`--ratio-hi/--ratio-lo/--z/--min-dev` 可调，
`--strict` 时离群也返回非零）：一条线里某区间远快或远慢于本线常态即异常，
例如修复前昌平线 `昌平东关→昌平` 达 73 km/h（本线中位 39）。

### `check_reverse.py` — 反向导出对比

把运行图在每个站的停站时刻反向导出成时刻表，与原时刻表逐站比对（默认 ±2 分钟）。
当前支持率约 96%，不一致处即解析或反推的疑点。

## 运行图的特殊处理

### 终点站补齐

终点站的站牌只印**反方向**的发车时刻，因此没有到达数据，运行图原本在倒数第二站结束。
现在：列车若已到达"最后一个有站牌的车站"，就继续运行到终点站（`MAX_TERMINUS_HOPS`
控制最多外推几站）；在中间折返的小交路车不受影响，仍然提前结束。
**目前 105 个分组全部抵达终点站**（修复前 88 个未抵达）。

### 物理线路合并

1 号线与八通线、4 号线与大兴线**是同一条物理线路**，此前各自成组，导致两组都不覆盖
对方区段（四惠东↔高碑店之间没有车）。现在按**物理线路**合并建图，1 号线贯通为 35 站。

### 区间用时双向对称

两个方向独立校准，其中一侧站牌有噪声时匹配器可能落到相邻班次槽位（1 号线
`大望路→四惠` 曾算成 5 分钟，反方向只要 3 分钟，地图上能看到列车在这段爬行）。
现在对**两个方向相差 ≥2 分钟的区间**取较短值作为两个方向的共同用时并重建相关分组。
`check_speed.py` 也会报告仍然存在的双向不对称区间（默认阈值 1.5 分钟）。

### 甩站（临时通过不停车）

`八角游乐园`（1 号线）目前**临时甩站**：官方站间距表里 古城→八宝山 是直连的 3874 m，
OSM 关系里也没有该站。解析出的该站时刻表仍保留在 `output/timetables.jsonl`（图片确有此表），
但**运行图会跳过它**（`infer_diagram.CLOSED_STATIONS`），列车由八宝山直达古城。

`build_web_data` 另有一道兜底：任何在当前线路几何里没有坐标的车站都会被移出该线路站序，
避免列车在它两侧"消失"。

### 首都机场线（机场端单向环线）

机场端是**单向环线**：`北新桥 → 东直门 → 三元桥 → 3号航站楼 → 2号航站楼 → 三元桥 → 东直门 → 北新桥`。

- 去程（开往 2 号航站楼）：三元桥 → **3 号航站楼 → 2 号航站楼**（终点）
- 回程（开往 北新桥）：**3 号航站楼 → 2 号航站楼 → 三元桥** —— 从 2 号航站楼**直达三元桥，不再经过 3 号航站楼**

OSM 关系里把 2 号/3 号航站楼的顺序写反了，已用 `infer_diagram.ORDER_OVERRIDE` 修正；
官方站间距表用的是 `T3航站楼`/`T2航站楼`，与站名不一致，已补别名；
环线回程段（T2→三元桥）站间距表里没有，用 OSM 直线距离 ×1.15 兜底。

### 同一站的重复站牌

首都机场线每一站都贴了两张牌：**「发车时刻表」**与**「列车时刻表」**，方向与运营日相同、
内容几乎一致（例如三元桥两张表首班都是 06:55）。同一站同一方向同一运营日的多张表若做**并集**
会重复计数（三元桥 88 + 90 → 178 班）。

`infer_diagram` 在合并时按"**来自不同站牌、时刻相差 ≤1 分钟**"视为同一班车只保留一条；
同一张牌内部相隔 1 分钟的两班车是真实存在的高峰加密，予以保留；两张牌互补（覆盖不同时段）
时仍然取并集（如 13 号线西直门）。若同一站牌被重复下载，保留**文件时间较新**的一份。

## 运营日

站牌上的运营日标注各线不同，代号也分开：

| 站牌标注 | 代号 | 适用 |
|---|---|---|
| 工作日 / 双休日 | `weekday` / `weekend` | 普通线路，周一–周五 / 周六日 |
| 周一至周四+双休日 / 周五 | `mon-thu_weekend` / `friday` | 首都机场线 |
| 平日 Ordinary Days | `ordinary` | 大兴机场线，非周五的每一天 |
| 重点保障（星期五及重点保障时期） | `key_guaranteed` | 大兴机场线，**周五**及重点保障期 |
| 每周日 23:00 后延长 | `sunday_extension` | 大兴机场线周日夜间 |

地图按**星期几**选择运营日，每一天自动汇总当天开行的所有类型：

| 星期 | 采用的时刻表 |
|---|---|
| 周一–周四 | 工作日 / 周一至周四+双休日 / 平日 |
| 周五 | 工作日 / 周五 / 重点保障 |
| 周六 | 双休日 / 周一至周四+双休日 / 平日 |
| 周日 | 双休日 / 周一至周四+双休日 / 平日 / 周日延长 |

## 地图实现（`web/index.html`）

- **纯静态单文件**：无构建、无依赖，数据来自 `web/data/network.json`。
- **停站动画**：把时刻表的分钟视为**到站时刻**——列车在第 M 分钟整（0s）到达，**停站 1 分钟**后
  （60s）发车，再用剩余时间运行到下一站。因此地图上能看到列车进站停留再启动，
  底部运行图呈标准**阶梯形**（水平段=停站，斜线=区间运行），点击列车后逐站时刻按
  `到达→发车` 显示。停站时长由顶部常量 `DWELL = 1` 控制。
- **在线列车数折线图**：侧栏「实时统计」下方显示当前运营日、当前高亮线路组合下
  全天每 5 分钟的在线列车数（4:00–25:00），红线标出当前时刻并给出读数与峰值。
  实现见 `trainSeries()` / `drawHist()`。
- **线路高亮**：点击线路标签高亮该线（其余淡出到 16%），折线图只统计被高亮的线路；
  支持多选，全部取消即恢复显示全网。

## 校验脚本与运行图的一致性

校准与检查使用同一套口径：站间用时 = 停站 1 分钟 + 运行时间，
且必须落在由官方站间距推出的物理区间内（`DWELL_MIN/MAX`、`MIN_SEG_KMH`、`MAX_SEG_KMH`）。
因此 `check_speed.py` 通过意味着运行图在物理上自洽，而 `check_reverse.py` 通过意味着
它与原始时刻表一致。
